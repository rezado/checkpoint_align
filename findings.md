# Findings

- 两套 profile 都有 `bin/cfg/profiling/cluster/checkpoint/json/stamps`，共有 57 个 workload JSON。
- 两边 profiling 日志显示 interval 为 20,000,000；`namd` A 总指令数为 1722583019975，B 为 1725110728365。
- `namd` A 的 `simpoints0` 有 64 个点，B 有 23 个点，点集合和 cluster id 不可直接对应。
- A/B `namd.elf` SHA-256 不同，但均保留 `.debug_*` 和 `.symtab`，可优先使用 DWARF/符号构造语义锚点。
- A 配置包含 `-march=rv64gc_zba_zbb_zbs_zbc`；B 配置使用 RVA23 相关 `MARCH` 且无向量选项，机器代码布局不可假设一致。
- A 的 point 是动态 interval 编号；报告应将语义锚点及动态 occurrence 作为跨 ELF 坐标，指令数比例只作 sanity check。
- 2026-09-07：mcf A 侧有 22 个实际 checkpoint，目录点集合与 A `simpoints0` 的 22 个点完全一致；现有 `align` 已为每点保留 best B candidate，但 `checkpoint` 只能单点执行且默认只允许 accepted candidate。
- 用户要求是覆盖 A 的所有 checkpoint，而非只处理一个 accepted 示例；批处理必须区分“为全部 A 点给出候选关系”和“该关系已通过置信度/restore 验证”。
- 2026-09-09：mcf `checkpoint-all` 已实际完成 22/22 个 B checkpoint（约 5.9 GB），逐对验证为 1 个 `validated_experimental`、21 个 `rejected_post_restore_divergence`；新 slice 导出必须允许按验证状态筛选，不能默认把 21 个失败项包装成可靠切片。
- 新 slice 应使用 B target point 作为 checkpoint 目录/`simpoints0` 位置，沿用 A cluster id 仅作 slice identity；位置对齐不读取或重算权重，导出也不声明 B 代表性权重。
- 2026-09-09：正式切片归档的 JSON schema 由 `scripts/checkpoint/step_metadata.py` 生成，结构为 `{workload: {insts: string, points: {point: weight_string}}}`；归档通常命名 `json/checkpoints_all.json`。用户明确要求 A/B 文件夹内各写 `checkpoints.json`，因此沿用该主体 schema 和指定文件名。
- 成对 A/B 切片中，A 使用原 A point 和 A SimPoint weight；B 使用对齐后的 B point，并继承对应 A cluster 的 weight，以便成对实验采用同一权重。该 weight 不是 B 独立聚类权重，必须在 manifest 中标明；位置匹配算法仍不读取权重。
- 2026-09-11：新目标将主流程切换为动态工作进度：位置身份是 `(build_id, anchor_id, occurrence, event_phase)`，BBV/总指令比例只能用于候选召回和 B 内覆盖，不能判定语义身份。
- 当前 checkout 在 `main`，已有 `cross_elf_checkpoint.py`、其测试和两份文档的未提交增量；这些属于用户现有工作，必须保留。目标明确要求在现有改动上增量实现，因此当前 checkout 可继续使用，无需 worktree。
- 现有仓库已具备 `dwarf_source.py`、`qemu_occurrence`、M0-M3 fixture 和 interval/BBV 兼容 CLI；新动态主流程应新增独立薄入口，并保持旧入口只输出兼容/候选语义。
- `position_aligner` 当前公开的是 BBV M1 的 `build_index/align`，已有候选窗口、gap/warp、严格单调 DP、top-2、预算与 typed reject 基础；应保留为兼容实现，同时把新的动态 trace 深接口放在包顶层。
- M0 已有一套 synthetic schema/gold 校验器，但冻结的是旧命名 `SourcePosition/PositionCorrespondence/AlignmentResult`，还需把 `ProgressEvent` 及目标要求的四个核心对象作为真正运行时协议实现，并添加禁止 weight 字段等负例。
- QEMU occurrence 已有稀疏 watchlist/二进制流基础；实现重点是把 collector 输出规范化为 ProgressEvent，并把退出状态、stdout/stderr、terminal marker、单 vCPU、完整流和稳定性纳入运行清单/验收。
- `local-src/NEMU` 已有未提交的 semantic-position 增量，支持 `trace-window` 和 `checkpoint-on-occurrence`、before-instruction 命中、有限 recent-event deque；这些改动与用户目标直接重叠，保持原样并由新 materializer 生成配置/sidecar。
- 真实保存的 `libquantum A28258->B28259` 证据包含 23,501 个 A/B QEMU events、A NEMU from-scratch occurrence 6594、B occurrence 6594/PC 75036、压缩 checkpoint 和 terminal restore；适合验证新 CLI 的真实跨 ELF适配，无需重跑长仿真。
- 新 CLI 真实回放确认 A SimPoint 请求 icount 565160000000 吸附到 A event icount 565177663589，delta +17663589；动态序列 canonical identity 投影到 B occurrence 6594/PC 75036。位置状态必须是 snapped，不能因为事件对应 exact 而抹去 A 边界位移。
- `lbm` 短程真实 E2E（1 timestep/no obstacle）A/B 各 3 事件、重复 2 次、terminal marker 命中、stdout/trace 稳定；A/B `main` 和 `LBM_showGridStatistics` 语义序列一致，PC 72476 vs 72484。
- `lbm` B main occurrence 0 已由 NEMU 从 workload 起点命中：icount 18,949,272、before-instruction PC 66368，checkpoint SHA-256 `3435df1565f13dc9ab895eecb3a5d9f708926edb91490ab038f050864bca9b6b`，zstd passed；bounded restore 40,000,003 instructions/good state passed，但未到 terminal marker，不能称完整 workload restore。
- P5 三个独立 validator 已实现；真实 libquantum M3 terminal restore 仍是保存证据，真实 lbm 仅有 bounded restore，因此 `production_eligible=false`。
